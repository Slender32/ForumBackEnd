<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.slender.forumbackend.mapper.ArticleLikeMapper">
    <select id="selectByArticleAndUser" resultType="com.slender.forumbackend.model.entity.article.relation.ArticleLike">
        SELECT article_id, user_id, create_time FROM article_likes
        WHERE article_id = #{articleId} AND user_id = #{userId}
    </select>
    <select id="selectArticleIdsByUser" resultType="long">
        SELECT article_id FROM article_likes
        WHERE user_id = #{userId}
          AND article_id IN
          <foreach collection="articleIds" item="articleId" open="(" separator="," close=")">#{articleId}</foreach>
    </select>
    <delete id="deleteByArticleAndUser">
        DELETE FROM article_likes WHERE article_id = #{articleId} AND user_id = #{userId}
    </delete>
</mapper>
